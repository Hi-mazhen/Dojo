pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}
rootProject.name = 'dojo'
include 'dojo-api'
include 'dojo-core'



rootProject.children.each {
    it.buildFileName = it.name + '.gradle'
}